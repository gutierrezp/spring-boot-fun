describe("Greeting Suite", function() {
	
	
	//This will be called before running each spec
    beforeEach(function() {
        calc = new MathUtils();
    });
    
	it("test click", function() {
        expect( true ).toEqual(true);
    }); 
});